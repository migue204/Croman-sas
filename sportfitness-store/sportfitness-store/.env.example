# Copia este archivo como .env.local y llena los valores reales.
# .env.local NUNCA se sube a GitHub (ya está en .gitignore).

# Ve a Supabase → Settings → API para conseguir estos dos valores
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Se llenan cuando conectemos pagos (Fase 3)
WOMPI_PUBLIC_KEY=
WOMPI_PRIVATE_KEY=
